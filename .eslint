module.exports = {
  extends: 'airbnb',
  // NOTE: you DO NOT need the prettier exension installed in VSCode.
  // Just reference it here and install eslint-plugin-prettier as a dev dependency.
  plugins: ['prettier'],
  "rules": {
    // This is here because the course examples put JSX in files with js extensions
    // but that's not allowed by the airbnb rules.
    // I'm overriding the rules to be able to follow the course materials.
    "react/jsx-filename-extension": [1, { "extensions": [".js", ".jsx"] }],
  }
};
